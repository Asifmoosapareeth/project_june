import 'package:flutter/material.dart';

class Register_Hive extends StatelessWidget {
  const Register_Hive({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
